{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}
{% block section_title %}{{ title }}{% endblock %}

{% block body %}
    <div class="mt-5">

        {{ form(form) }}

        <a href="{{ path('index_veterinarian') }}">
            <button class="btn btn-danger w-100">Cancelar operação</button>
        </a>

    </div>
{% endblock %}
